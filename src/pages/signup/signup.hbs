<main>
  <form action="">
    <div class="register-window">
      <h3 class="w-100 d-flex justify-center">Регистрация</h3>
      <div class="fields-block">
        {{> Input name="email" placeholder="Почта" }}
        {{> Input name="login" placeholder="Логин" }}
        {{> Input name="first_name" placeholder="Имя" }}
        {{> Input name="second_name" placeholder="Фамилия" }}
        {{> Input name="phone" placeholder="Телефон" }}
        {{> Input name="password" placeholder="Пароль" }}
        {{> Input name="password_confirm" placeholder="Пароль (еще раз)" }}
      </div>
      <div class="d-flex flex-column valign-bottom h-100">
        <div class="w-100 mt-auto">{{> Button id=button.id className=button.className text=button.text}}</div>
        <div class="w-100 d-flex justify-center mt-4">
          <a id="signInButton" href="#">Войти</a>
        </div>
      </div>
    </div>
  </form>
</main>
