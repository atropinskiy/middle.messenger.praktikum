<main>
  <form action=''>
    <div class='register-window'>
      <h3 class='w-100 d-flex justify-center'>Регистрация</h3>
      <div class='fields-block h-100'>
      {{#each this}}
        {{#if (neq @key "submitButton")}}
          {{{this}}}
        {{/if}}
      {{/each}}
      </div>
      <div>
        {{{submitButton}}}
      </div>
    </div>
  </form>
</main>
