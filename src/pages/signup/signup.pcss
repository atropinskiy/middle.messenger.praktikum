.register-window {
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  width: 340px;
  height: 615px;
  padding: 50px 30px 30px;
  border-radius: 12px;
  box-shadow: 0 0 6px rgb(0 0 0 / 14%);
}

.signup-button {
  margin-top: 150px;
}
