export enum ROUTER {
  signin = "/signin",
  signUp = "/signup",
  chat = "/chat",
  profile = "/profile",
  profileEdit = "/profile-edit",
  errorPage = "/errors",
  passwordChange = "/change_password"
}
